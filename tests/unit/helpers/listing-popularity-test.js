
import { listingPopularity } from 'crowd-sourced-review-site/helpers/listing-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | listing popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingPopularity([42]);
  assert.ok(result);
});

